Identification
--------------
- Array/string ka question hoga
- Subarray or substring ki baat ho rahi hogi
- Fixed size window me k diya rahta tha jo fixed size window tha but variable size window sum diya hoga or
  some condition

- isme window size ki baat hogi but diya nahi hoga

Logic
----
same as fixed size start with
input k = 5 and arr = 4,1,1,1,2,3,5
i =0 adn j=0
size = arr.length
long sum =0;
int max = o
while( j < size)
{
 sum  = sum + arr[j]
  if(sum < k )
    j++
  else if( sum ==k ){
     if( j-i+1 > max) max = j-i+1
  }
  if(sum > k){
   while(sum > k){
   // jab tak sum > k has tab tak piche wala number hato matlab minus karo
     sum = sum - arr[i]
     i++;
   }
  }
  j++;
}
return max;


//Fixed size window
while(j<size)
{
  //calculations

  if(windowSize < k){
    j++;
   }
   else if(windowSize == k){
     ans <- calculation
     calculation remove i
     window size maintain and slide
     i++ ,j++
   }
}
return ans
